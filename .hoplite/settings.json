{
  "version": 1,
  "ports": {"preview": 3000, "additional": {}},
  "scripts": {
    "setup": {
      "enabled": true,
      "command": "if ! command -v git-lfs >/dev/null 2>&1; then apt-get update && apt-get install -y git-lfs; fi && npm ci --no-audit --no-fund"
    },
    "run": {"enabled": true, "command": null},
    "archive": {"enabled": true, "command": null},
    "check": {"enabled": true, "command": null}
  },
  "mcpServers": []
}
